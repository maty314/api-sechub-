apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-sechub.fullname" . }}
  labels:
    {{- include "api-sechub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-sechub.selectorLabels" . | nindent 4 }}
